import { Story } from '../types';

// Compatibility export for the remaining admin link editor. Public articles
// are served from D1; the legacy code-defined magazine catalog is retired.
export const STORIES: Story[] = [];
