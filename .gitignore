node_modules
dist
.env.local
.dev.vars
.claude/
worker/.wrangler/
test-results/
playwright-report/

# Agent brainstorm scratch (never commit)
.superpowers/

# Isolated agent implementation worktrees
.worktrees/

# Temporary third-party web research captures
.firecrawl/

# Agent critique scratch (never commit)
.impeccable/

# Local sandbox database dumps (a copy of live product records, pulled on demand
# by `npm run sandbox:refresh`). Never committed: they are large, they go stale,
# and the sandbox is meant to be rebuilt, not tracked.
worker/sandbox/*.sql
worker/sandbox/*.json
worker/sandbox/table-list.txt
