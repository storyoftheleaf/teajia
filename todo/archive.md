# Teajia TODO — Archive

Finished work, moved here from TODO.md. Nothing is deleted; lineage is kept.

## Creator profiles (2026-09-21)

- [x] Creator profiles: the public page at /people/:slug, the People directory at /people, and Pay kept private behind a permanent approval, done 2026-09-21, last commits 13475e88 (the cover owns the first sentence) and 4b747207 (Selected by names the person); People joined the sidebar rail in 7b92e38a. The page is in the tea master's first person and follows the Read section's design: sticky nav with the person's name small on the right, portrait cover with the one line, up to three cells (Words, Teas, Pay), then In my words, Hands on, Words, The teas (three shown, the collection is the destination), Hosting, My table, Reach me; sections the person has not filled are absent, not empty. Pressing Pay shows a gate; asking needs an account; the request lands at the tea master's Your Table, Decline left, Approve right; approval is permanent, no revoke and no "I have a link" step; share links minted per invoice or from Your Table open the pay sheet with the invoice amount and tea count; nothing with a bank number is ever public. Quoted in opens the article on the lit passage. Specs: `tests/creator-profile-{sparse,full}.spec.ts`, `creator-pay-gate.spec.ts`, `creator-directory.spec.ts`, `worker/tests/pay-is-private.test.ts`. Canvas and the shipped record: [creator-profiles.md](plans/creator-profiles.md). Still open in TODO.md: real photos, and the tea master's own editing page.

## Prime-time audit (2026-09-09)

- [x] Every new tea ships free and carries the old markup through five of six doors, because the live table still defaults to 0 and 2.5, done 2026-09-09, "name freight, markup and cost at every door". JOBO-1, MONEY-1, FEEL-1, MONEY-7. Reproduced against a database seeded from the migration ledger rather than from `worker/schema.sql`, which is the whole reason this ran for months: schema.sql says `products.shipping_rate_per_kg REAL DEFAULT NULL` and the live column, created by migration `0000`, says `DEFAULT 0`. Ten INSERTs across `index.ts`, `mcp.ts` and `curateImports.ts` reach `products` and `product_listings`, and five of them named none of the three columns, so each landed 0, 2.5 and 0: a tea that ships free, prices at the multiplier `0013` cleared off the shelf, and cost nothing. Four more doors than the plan listed were found by searching for the shape rather than the symptom, including two that write listings directly (carrying a partner's tea, receiving a wholesale order) and the Curate import, which was landing free teas outright. Every door now either names the three columns in its SQL or passes its column bag through `nameProductColumns`; the bulk import calls it AFTER its empty-cell strip, since before it the strip simply removed the answer again. The behavioural test drives the form, the bulk import and the agent door against a migration-seeded database, and `schema-defaults-are-decisions.test.ts` now reads each registered default out of the ledger and compares it with the reason written beside it, so a default that disagrees with its own explanation fails the suite. **The table rebuild is migration `0018` and it is NOT in the code commit**: it goes to Adrian as a page first. Worth recording that the obvious rebuild is dangerous here and was measured to be: `DROP TABLE products` on real D1, which refuses `PRAGMA foreign_keys = OFF` and honours only `defer_foreign_keys`, cascade-deletes `article_products` and `sales_grants` and aborts on `inventory_receipt_lines`' RESTRICT. `0018` changes the column in place instead and touches no other table.

- [x] 21 live teas carry a currency label the admin cannot resolve; canonicalise once in the admin, stop `update_tea_pricing` uppercasing, and correct the rows, done 2026-09-09, "one alias map, and no rate of 1". JOBO-2, MONEY-5, MONEY-10. The exchange table keys yuan as `Yuan`, so 'CNY' and 'Yuan' are the same money spelled twice, and the map that knew it lived inside the worker where the admin could not reach it. The worker canonicalised and the shelf price was right all along; the admin did eleven exact `===` lookups and fell back to a rate of 1, so on 22 real teas the dashboard counted a 1,200 yuan cost as $1,200 instead of $178, the inventory preview priced them at nothing at all, and a freight rate pinned at 85 yuan would have read $85.00 against a true $12.63. The map has one home under `src/` now and the worker imports it, which is the direction this build already allowed; a currency that will not resolve returns null and the surface shows a dash, never a 1. Two more copies of the same mapping turned up while fixing it, in the Tea Compass and the dashboard's rate list, and the guard found a third class of the same fault: the shop was refusing 'CNY' as a currency it keeps no live rate for while refreshing that exact rate daily under the name 'Yuan'. **The row correction is migration `0017` and it is NOT in the code commit**: it goes to Adrian as a page first, though it moves no price, only a spelling. Repaired on review the same day: the guard only knew the object-literal shape, so two more live copies written as `includes()` ladders had been sitting under `src/admin/lib` the whole time it was passing, one of them missing AUD and MYR so a sheet in either was refused row by row; removing `.toUpperCase()` at the three doors without moving case-normalisation into the map had made `canonicalCurrency('hkd')` answer 'hkd', a spelling `exchange_rates` matches nothing against; the intake workspace answered `NaN` for a currency it could not read and its only reader took `extraCost || 0`, so a line lost its whole share of the shipping in silence; the dashboard dropped such a tea from the RETAIL totals as well, though those consult no rate; and a local helper in `curateImportAnalysis.ts` shared the name `canonicalCurrency` while doing the opposite job. Round three, done 2026-09-09: the guard only ever read `mcp.ts`, so four REST product write doors (single create, bulk create, the product edit/commercial update, and the Tea Compass promotion) were still storing whatever casing a caller sent straight into `products` and its listing mirror, reproduced against a seeded database on 'cny', 'CNY' and 'hkd'. One `canonicalizeCostCurrency` helper now sits beside the existing cost-currency guard in `worker/src/costCurrency.ts` and all four doors call it before their INSERT or UPDATE runs, and the guard now reads `index.ts` too. The dashboard's `costlessCurrencies` count was computed and never shown, so a tea whose cost currency would not resolve just vanished from the cost total with nothing on screen; it now renders in plain words with a link to the Currency page. The Tea Compass's own three-symbol map and the dashboard's own seven-currency name table were both dropped for the shared ones in `src/lib/currency.ts` and `src/utils/currency.ts`. The guard's exemption for `PartnerListingEdit`'s legitimate rate-of-1 (converting a curator's own USD price into USD) was keyed on a string appearing anywhere in the file; it is keyed by path now. And the freight refusal message read "1 in XYZ" for both one line and several; it reads "1 line in XYZ" and "3 lines in XYZ" now. Four customer-facing siblings that show the wrong figure for a yuan or Taiwan-dollar order (`src/lib/orderMoney.ts`, the worker's local-payment lookup, `SourcesView` and Tea Compass `OrderSummary`'s own symbol tables, and the order-email `formatMoney`) were left alone and filed to TODO.md's Untriaged, because they degrade honestly and touching customer-facing display is Adrian's call.
- [x] A blank cost becomes zero at four client doors (CSV, xlsx, sample graduation, Tea Compass), and the agent-door guard test cannot go red, done 2026-09-09, "a blank cost is a cost nobody entered, at every door". MONEY-4, JOBO2-1, MONEY-12. Each of the four browsers turned an empty price into 0 before the request left the page, so the server's guard was handed a well formed zero, agreed that a zero needs no currency, and let it through: reproduced against a migration-seeded database, a row with a blank price and a currency of HKD landed at `cost_amount` 0 and priced at 0. One reader now answers that question for every import cell, and nothing written stays nothing written. The bulk door refuses the ROW rather than the request, because a spreadsheet is many teas and one empty cell must not throw away the rest of the file. And the guard itself was decoration: it matched source text, so deleting the `throw` in `create_tea` left all 17 of its tests, and all 1,407 others, green. It drives the three create doors now.

- [x] Stamp cost currency provenance on all eight write paths and both listing mirrors, BEFORE the per-vendor backlog is run, done 2026-09-09, "stamp a stated cost currency at every door". MONEY-6, JOBO2-2. Two doors stamped and four did not, so a tea imported with a correct HKD cost sat in `list_unstated_costs` and a vendor-wide answer of yuan would have overwritten it and moved its shelf price. A fifth fault was found in the same place: the update path stamped BEFORE its unknown-field gate, so all three product command routes refused any edit naming a cost currency with a 400 about a column the server had just added itself. One helper decides the answer now, the mirrors copy it rather than re-deriving it, and the guard refuses a new insert that names a currency without it.

- [x] The wholesale catalogue quotes cost times markup as a per-gram price without dividing by quantity - done 2026-09-09 (fix(network-catalog): divide the curator's cost by quantity before the markup). MONEY-3: 89 of 92 reachable listings, up to 2,500x. `handleNetworkCatalog` now selects `product_listings.quantity_purchased` and divides `curator_cost_amount` by it before applying the markup, matching the shape `addPricingFields` already used for the shop's own catalogue. A profile with no recorded quantity now returns no cost-based price rather than pricing the whole batch as a single gram. Covered by `worker/tests/network-catalog-per-gram.test.ts`.

- [x] Nothing on a push to main runs the type check, the build or the full test suites; add the gate that would have caught the two-day outage - done 2026-09-09, `ci(playwright): the gate lands, six known failures quarantined visibly`. TEST-1, TEST-2: `deploy-frontend.yml` was manual, `playwright.yml` ran 2 of 48 specs, no hooks. `.github/workflows/playwright.yml` now runs, on every push and pull request to main: `checks` (tsc, the color lint, the production build, the worker suite), `mobile` (unchanged), `e2e` (the full browser suite on Desktop Chrome and Mobile Chrome, dist built first), `china-scan`, `platform-hardening` and `recovery` (the three suites that had no CI lane at all). Reproduced the outage by reintroducing the two JSX comments from the parent of `2f1b7ea3`: `npm run lint` and `npm run build` both fail exactly as they did on 2026-09-06 through 2026-09-08. `tea-reference-preview.spec.ts` and `tea-reference-revision-workflow.spec.ts` stay out (already excluded by `playwright.config.ts`'s own `testIgnore`, and the preview spec needs a private, non-CI handoff file). Running the full suite for the first time surfaced six specs failing independently of this change (`compass-capture.spec.ts:498`, `inventory-scroll.spec.ts:220` and `:275`, `shop-refinement.spec.ts:113`, `smoke.spec.ts:44`, `tasting-note-curation.spec.ts:87`). Reproduced again on this checkout: the six run and fail (11 of 12 runs, one flaky pass on `inventory-scroll.spec.ts:220` under Mobile Chrome, matching the original count). Their titles now sit in the workflow-level `KNOWN_FAILING_E2E` regex; `e2e` runs with `--grep-invert` against it so every other test, including the rest of `inventory-scroll.spec.ts`, still blocks a merge (proved by running that file alone with the same invert: 11 passed, 1 skipped by its own project guard, the two known-failing titles correctly absent), and a new `known-failing` job runs the same six with `--grep` and `continue-on-error: true` so they stay visible rather than going quiet. Each of the six is also filed under TODO.md's Untriaged section with what a visitor or operator would see broken; the two-Save-buttons, empty-currency-select, missing-dollar-figure and missing-sticky-search-bar lines read as real page defects rather than stale tests, and the other two (the stock page's missing selection-toolbar actions, the promoted note's missing byline) stay unclear pending a look at the current page.

  A second pass fixed four things a reviewer found on the first landing. The `e2e` job now shards four ways (`--shard=N/4`, `fail-fast: false`, `timeout-minutes: 25` per shard) because the unsharded suite measured 35.0 minutes of playwright time alone on faster hardware than this private repo's 2 vCPU runner tier gets, well past the original 30-minute cap; `known-failing` is unaffected. The `recovery` job now runs `npm run build` once before Playwright starts, so `playwright.recovery.config.ts`'s own `npm run build && npm run preview` webServer, left untouched, warms into a 120-second budget instead of racing it cold. Worker types remain ungated: `npm run lint` covers `src` only and `wrangler deploy --dry-run` strips types via esbuild, so a type error in a worker file no test imports still lands green; `tsc -p worker/tsconfig.json` reports 35 pre-existing errors across seven files today (`curateImportAnalysis.ts`, `curateImportCanonical.ts`, `curateImports.ts`, `eventArticleDraft.ts`, `eventDomain.ts`, `index.ts`, `mcp.ts`), filed as its own TODO line rather than fixed here. And the archive line you are reading used to say two of the six looked like real page defects where TODO.md said four; it now says four, matching TODO.md.

- [x] Draft articles are readable at their direct URL; hide the fourteen article pages from visitors until they are published (JOBC-2), done 2026-09-09, `Gate every surface that names a Read article on one check`. `ReadIndex.tsx` was the only place that knew which pieces were live, and it only decided what the contents list showed, so each of the fourteen pages was fully readable at its own URL: a visitor with empty storage could read the whole Rock Remembers interview at `/read/rock-remembers`. The live map moved into `src/pages/read/articleLive.ts` and every surface that NAMES an article now asks it: the route itself through a new `ArticleGate` in `App.tsx`, the contents index, the "More from The Art of Tea" rail at the foot of each piece, and the crawler-facing head meta the Pages function writes. Flipping a piece live there publishes it everywhere in one edit. `/read/leaf-to-liquor` and the index itself sit outside the map deliberately, named once in `UNGATED_READ_PATHS` rather than remembered separately by three callers. Two rounds of review found the surfaces the first pass missed. The rail was the worst of them: it was a hand-curated array per page with no publish check, so `/read/porcelain-and-tea` advertised three unpublished pieces by title and blurb and every one of those cards was a dead link onto the not-found page the gate had just started serving. Also closed: an ungated route written across several lines was invisible to the route sweep, a crawler-meta entry with no live-map entry kept its own title and description, and a draft still told crawlers its own URL was canonical. The owner check was over-granting too, letting any curator who owns their own shop read every Teajia draft; it now takes the Teajia account, its publish bundle, or a platform role. What this does NOT do: it hides a draft from a visitor, it does not make one private. The text still ships in the public JS bundle and the owner check runs in the browser, so anyone determined can still read an unpublished piece. That is recorded as its own open item in `TODO.md`, because closing it means moving the check and the content server-side.

- [x] Give `create_tea` a real tea type default, done 2026-09-09, "refuse a guessed tea type in create_tea instead of defaulting to 'Tea'". MONEY-8. `create_tea` defaulted an omitted `type` to `'Tea'` and its own tool description told the model to try `'Pu-erh'`, and neither word is in `TEA_TYPES` (Green, White, Yellow, Oolong, Red, Dark, Sheng, Shou, Herbal, in `src/wisdom/vocabulary.ts`), the exact list `authorizeInvoiceLines` (`worker/src/teaMasterSales.ts`) checks before it will let a product sell. Reproduced against a seeded database: a product row with `type` 'Tea' is refused by `record_sale` with `product_not_sale_eligible`, so a tea created through the agent door with no type looked like an ordinary row right up until the first attempt to sell it. The tool now refuses an omitted or unrecognised type by name, listing the types the shop actually accepts, the same shape `createMissingCost` already uses for an omitted cost; a recognised alias (`normalizeTeaType`, e.g. 'Black' or 'Wulong') is accepted and stored under its canonical word, and bare 'Pu-erh' stays refused on purpose because the vocabulary module itself cannot resolve it to Sheng or Shou without a human. The tool schema now enumerates the nine accepted types and marks `type` required, and `required` on the tool definition names it. Two other tests (`a-tea-arrives-with-its-cost.test.ts`, `a-stated-currency-is-marked-as-stated.test.ts`) were calling `create_tea` with the placeholder type `'Pu-erh'` for unrelated cost/currency assertions; updated to `'Sheng'` so they keep testing what they always tested. Left alone as out of scope: `worker/src/curateImportAnalysis.ts`'s AI-curation prompt already names the same nine types correctly in English (a second copy of the wording, not the enforcement); `src/components/TeaCompass/index.tsx` and `types.ts` carry their own duplicate of the type list for that form's local validation; `worker/src/curateImports.ts` falls back an uncategorised item to `'Misc'`, an honest non-tea catch-all rather than an invented tea type. Covered by `worker/tests/create-tea-needs-a-real-type.test.ts`.

- [x] Rate-limit and length-cap the inquiry and newsletter endpoints, and make an absent limiter binding refuse rather than allow. Done 2026-09-09, commit "fix(inquiries): rate-limit and length-cap the public write endpoints", repair round commit "fix(inquiries): brake every absent-binding site, not one helper". SEC-1, SEC-2, SEC-5. `POST /api/inquiries` and `POST /api/newsletter/subscribe` now key a durable limiter (`INQUIRY_LIMITER`, `NEWSLETTER_LIMITER`, both declared in `worker/wrangler.toml`) off the client IP, and every field on both endpoints is capped, the consult branch's `vision`/`interests`/`referral`/`location`/`whatsapp` and the cart branch's `customer_location`/`phone`/`ref_number` included, which is the field that had stored a 2 MB message uncapped. Both handlers also refuse an oversized request before it is read into memory, via the existing `validateContentLength` guard. `enforceDurableLimit` in `worker/src/index.ts` now refuses (503, naming the missing binding in a `console.error`) when a caller's binding is absent instead of allowing the request through, and the same class fix now covers `enforceOAuthLimit` in `worker/src/mcp.ts` (guards the unauthenticated `/oauth/register` and `/oauth/authorize`) and the public MCP's own limiter check at `/mcp/public`, both of which had the identical absent-binding bypass. The four `LOGIN_LIMITER` sites in `worker/src/index.ts` were left alone on purpose: each falls through to an unconditional in-memory `checkRateLimit`, so an absent binding there loses the cross-isolate durability but never loses the brake entirely, which is a different risk from the ones fixed here. All callers were confirmed to already carry a `wrangler.toml` declaration before any of this changed, and their tests were given a fake open binding so the class fix does not regress their own coverage. A third round found the same absent-binding bypass survived in one more place the first survey missed: the public MCP's `prepare_order` tool (`worker/src/mcp.ts`) writes an `inquiries` row exactly like the endpoints above, but its `PUBLIC_PREPARE_ORDER_LIMITER` check read `if (env.PUBLIC_PREPARE_ORDER_LIMITER)` with no else and the binding was never declared in `wrangler.toml` at all, so it ran on an in-memory limiter alone. The binding is now declared (5 per 60s, matching the code's own in-memory cap) and `publicPrepareOrderRateCheck` refuses with 503 when it is absent, the same as every other caller. The round also restored the `Retry-After` header on the `/mcp/public` 429, dropped when that check was folded into the shared `enforceDurableLimit` helper; it is the one 429 in this file that ever carried the header, so `enforceDurableLimit` now takes an optional `retryAfterSeconds` used only by that one caller rather than added to all of them. A fourth round found `validateContentLength` itself adding the 1 MiB multipart allowance to the two JSON callers as well, so the 32 KiB inquiry cap and the 4 KiB newsletter cap each actually admitted well over a megabyte and only refused a body past that (reproduced: a 100 KiB inquiry body returned 201, not 413); the helper now takes an `overheadBytes` parameter defaulting to the multipart allowance so all seven multipart callers are unchanged, and the inquiry and newsletter handlers pass 0 so their stated cap is what is enforced. `publicPrepareOrderRateCheck` fails open when the limiter binding throws, a documented choice so a rate-limiter hiccup never blocks a real customer's WhatsApp link, while the two shared helpers, `enforceDurableLimit` and `enforceOAuthLimit`, fail closed on the same throw.

- [x] The shop grid price omits the handling fee the ladder charges; use one quote for both, done 2026-09-09, "fix(shop): quote the grid price through the ladder's curve". JOBC-1. Adrian's decision on the recommendation: the grid uses the same quote as checkout, so a customer never sees a number go up. Reproduced against the sandbox's own Mahei Gushu Red (`retail_price_per_gram_usd: 0.25`): the grid read `$25` at 100 g and the product page's own ladder read `$27` for the same 100 g, the exact gap the audit named. (The shop's default weight moved from 100 g to 50 g in `ccf31994`, an unrelated commit landed on `main` while this fix was in progress; re-verified after rebasing onto it, the same tea now reads `$15` on the grid and `$15` on the product page at the new 50 g default, screenshotted in `test-results/grid-price-fix/`.) `TeaLedger.tsx` multiplied price per gram by the weight directly; `quoteGrams()`, the product page's ladder, the cart and the order total all add the shop's flat handling fee and stop it at a whole piece or sealed unit. A new `quoteForDisplay()` in `src/lib/teaPricing.ts` is the one place a chosen weight becomes a quote for every reader-facing surface: it snaps a sealed-unit tea's weight up to the unit it can send (the same rounding `AlcoveCard`'s own slider already does) before handing off to `quoteGrams()`, so the grid, a favourited card or a bundled set can each ask for "50 g of this tea" and get back the same figure the checkout would charge, grams included, without re-deriving the whole-piece and sealed-unit logic themselves. `TeaLedger.tsx` now reads through it and shows the grams it actually snapped to, not the raw weight toggle, so a sealed tea's label never claims a weight the shop cannot send. The class fix found three more sites carrying the identical bare multiplication on customer-facing totals, all fixed the same way: the Starter/Teaware Sets add-to-cart and "you save" comparison in `Shop.tsx` (which also silently used a hardcoded 50 g regardless of the quantity actually configured on the set item, now reads that field); the saved/favourites card and its quick-add button in `CollectionTab.tsx`; and the JSON-LD structured data on `ProductPage.tsx` (`price`, `lowPrice`, `highPrice` on every quantity offer), where the mismatch is also a schema.org rich-results fault, not just a display one. Left alone and why: `ShopSearch.tsx`, `SharedCollection.tsx` and `MyCollection.tsx` show a bare per-gram shelf rate, never a total for a named weight, so there is no ladder figure for them to disagree with; `PublicCart.tsx` already reads `item.totalPrice`, computed upstream through the same curve when the item was added; the sample-request modal in `AlcoveModals.tsx` already routes its total through `quoteGrams` via the product page's own `formatTotal`. Proven red: reverting `TeaLedger.tsx` to the bare multiplication drops Moonlight White's grid price from $10 to $8 at the 50 g default in the new Playwright assertion, and reverting `quoteForDisplay()` itself fails all four of its new unit tests in `teaPricing.test.ts` (the sealed-unit snap included). New coverage: `tests/shop-refinement.spec.ts`'s "quotes the same total on the grid and the product page" opens Moonlight White from the grid and asserts the grid's figure equals the 50 g row's total inside the product page's own amount list (a `data-testid` added to that row's total span and to the grid's price cell, since neither carried one). The pre-existing quarantined test at `tests/shop-refinement.spec.ts:113` ("opens a tea into the product page, with one rendering per address") was checked against this finding and is not it: its dock-strip amount button reads "50g0.19/g" with no dollar figure in it at all, because a prior redesign moved the order total out of that control on purpose (its own code comment says so) and left the total to the Cart button's aria-label instead; the test's stale assertion is a separate, already-filed TODO line, so `KNOWN_FAILING_E2E` and its TODO entry are untouched.

## Orders and payments

- [x] The phone suite watches the screen edge, not just the page width - done 2026-08-31. The old check compared the whole page against the screen, so an element overrunning its column was absorbed without the page growing and passed unseen: the order summary rendered 551px inside a 311px column and carried the amount 208px off a 375px phone while the page measured exactly 375px. The new check asks whether anything a person can see reaches past the right edge, counting text and painted backgrounds only, because the blunt version flagged a shop tab strip whose box overhangs by 12px while painting nothing there. Surveyed across all twenty swept routes first and clean on every one, then promoted into the shared health assertion so it covers them all rather than the two money pages it started on.

- [x] Every way of being owed money ends at the same record — done 2026-08-31. Wholesale wrote two invoices carrying a status the app does not have, no line items and no amount, with the two money columns on the same row contradicting each other, so the ledger, the pay link and the attention list could not see the money at all. They are ordinary priced invoices now, marked fulfilled because receiving is the goods changing hands and unpaid because receiving tea is not paying for it, and receiving is refused outright when a line's currency has no exchange rate rather than invoicing it at par. The events half of that item was wrong and was read off the schema: `event_attendees.payment_status` is dead, nothing in the worker reads or writes it, and event money already runs through ordinary `EVT-` invoices. What is actually true about events is filed separately.

- [x] A sample request is recorded whether or not the tea is in the shop — done 2026-08-31. A line may be a named custom line with no product, the shape converting already produces for a retired tea. The product-id requirement had been doubling as the anti-junk gate on a public write, so the gate is stated properly instead: a custom line must say it is custom, at most 50 lines, a name at most 120 characters, a note at most 2000. Three of those four are tighter than what shipped before.

- [x] An order is not sent while a line sits at no price — done 2026-08-31. Draft to pending is the only path that makes an order a real ask, and it refuses there, naming the lines so the screen asks about them by name. "Send anyway" is one click, because the operator is the one who knows whether a line is meant to be free.

- [x] The pay page says which order the money is for — done 2026-08-31 → Plan: [order-process-handoff.md](plans/archive/order-process-handoff.md). It uses the key the customer already holds rather than a new one: they arrive from their own order page, so the same key one step later reveals nothing new, and a stranger who guesses a reference code still sees exactly what they saw before. The token never enters the address, because this page renders its own address as a QR code and hands it out under a heading inviting the visitor to share it. Four further problems were found and fixed in the same pass: the order body was staying out of durable browser storage only by a lucky substring match, the one line wiring the feature up had no test and deleting it left everything green, the recipient was not checked alongside the shop so a hand-edited link could show a real order above the wrong person's bank details, and the customer's private key was reaching the incident log through a path recorded verbatim. A fifth was found by opening the live page on a phone: the block sized itself to the longest tea name and pushed the amount 208px off a 375px screen, invisible to every test because the page reported no sideways scroll.

- [x] Close the loop from an order request to being paid — done 2026-08-31, live at teajia.com. An order request now reaches Adrian by email instead of waiting to be noticed, becomes a priced invoice in one click instead of being retyped, and carries a pay link resolved to the right tea master with the amount and a reference to quote. Payments got their own records, so a customer can report a transfer and Adrian confirms it against his bank: a report is never money, part payments work, and the pay link asks for the balance outstanding rather than the full amount again. What the customer sees on their tracking page is now derived from the order itself instead of a status kept by hand, which was generating the "where is my tea" messages the page exists to answer. What needs Adrian gathers into one list in Your Table, oldest waiter first, and six voice commands reach all of it. Fixed along the way: the fulfilment email had never sent because it looked for a column that does not exist and pointed at a domain Teajia does not own; marking an order paid the old quick way now leaves a ledger row so history cannot contradict status; sample requests and AI-prepared orders were vanishing into WhatsApp unrecorded; the cart survived checkout so an order could be sent twice; and the baseline schema carried a Cloudflare internal table that made a fresh database fail to build. Three rounds, ten agents. Remaining work is in [order-process-handoff.md](plans/order-process-handoff.md), which stays live. → Plans: [order-process.md](plans/archive/order-process.md), [round two](plans/archive/order-process-round-2.md), [round three](plans/archive/order-process-round-3.md), [cross-agent notes](plans/archive/order-process-notes.md)

- [x] Stop writing the payment ledger rules twice — done 2026-08-31. Eleven pieces of the money code had been copied from `worker/src/index.ts` into `worker/src/mcp.ts` under a "keep in sync" note, because index imports mcp and the reverse would close a module cycle, so a change to what counts as paid had to be made in both files. They now live in `worker/src/invoiceDomain.ts`, which imports neither, and both import from it: PAYMENT_EPSILON, the text limits and caps, roundUsd, paymentTextField, formatInvoiceNumber, loadInvoiceLedgerTotals, invoiceMoney, loadLedgerInvoice, reconcileLedgerWithColumn, recomputeInvoicePaymentStatus. The same pass closed the one place the two implementations disagreed: `whats_waiting` required `status = 'Draft'` across both halves of its unpriced question, so a Pending order carrying a line priced at nothing was on the screen and not in the spoken answer. `worker/tests/attention-parity.test.ts` now asserts the screen and the voice return the same list, and fails if either drifts.

## Curate

- [x] Make Curate deletes survive being offline — done 2026-06-29 via tombstones: a deleted id is recorded locally and filtered out of hydrate so the still-on-server row can't reappear; the delete is retried on each hydrate until the server confirms it. Fixes "I deleted it and it came back."

## Read / Magazine

- [x] Build the immersive article reader that replaces the 4:5 carousel as the default, where writing leads and visuals plus text-effects punctuate it, reading great on phone and desktop _(done 2026-06-17 — AR.0–AR.6 shipped: scroll-driven reader, section families, ~12 text-effects, block-stack authoring, share-card generation; merged into main)_ → Plan: [immersive-article-system.md](plans/archive/immersive-article-system.md)

- [x] Run the prime-time audit: every lane, verified findings, one ranked page (2026-09-09) → [docs/AUDIT-2026-09.md](../docs/AUDIT-2026-09.md), plan kept at [todo/plans/prime-time-audit.md](plans/prime-time-audit.md)

- [x] Remove the streak counter from the Learn curriculum - done 2026-09-09, "Remove the invented streak counter from Learn curriculum". OPS-3: `calculateStreak` in `LearnCurriculum.tsx` turned watched stories into a "day streak" by dividing the count by two, a number nobody chose sitting next to the real completed count. Streak trackers are on CLAUDE.md's DO NOT build list. Removed the function and its render; nothing else in `src` computes or shows a streak, checked by grepping for gamification patterns (badge, gamif, achievement, points) across `src` and `worker/src`, all of which turned out to be ordinary status pills or content-difficulty labels. Guarded by `worker/tests/no-streak-tracker.test.ts`, which scans every file under `src` for the word outside comments.
- [x] Choose a direction for the home page (2026-09-22: Cover C built as HomeV2Page, refined over two days, swapped in as home) → Plan: [home-page-directions.md](todo/plans/archive/home-page-directions.md)
- [x] Remove the old home page (2026-09-22: components/HomePage.tsx deleted, 500 lines, nothing imported it; its dead navigate-event listener in App.tsx went with it)
