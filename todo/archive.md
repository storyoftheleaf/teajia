# Teajia TODO — Archive

Finished work, moved here from TODO.md. Nothing is deleted; lineage is kept.

## Prime-time audit (2026-09-09)

- [x] A blank cost becomes zero at four client doors (CSV, xlsx, sample graduation, Tea Compass), and the agent-door guard test cannot go red, done 2026-09-09, "a blank cost is a cost nobody entered, at every door". MONEY-4, JOBO2-1, MONEY-12. Each of the four browsers turned an empty price into 0 before the request left the page, so the server's guard was handed a well formed zero, agreed that a zero needs no currency, and let it through: reproduced against a migration-seeded database, a row with a blank price and a currency of HKD landed at `cost_amount` 0 and priced at 0. One reader now answers that question for every import cell, and nothing written stays nothing written. The bulk door refuses the ROW rather than the request, because a spreadsheet is many teas and one empty cell must not throw away the rest of the file. And the guard itself was decoration: it matched source text, so deleting the `throw` in `create_tea` left all 17 of its tests, and all 1,407 others, green. It drives the three create doors now.

- [x] Stamp cost currency provenance on all eight write paths and both listing mirrors, BEFORE the per-vendor backlog is run, done 2026-09-09, "stamp a stated cost currency at every door". MONEY-6, JOBO2-2. Two doors stamped and four did not, so a tea imported with a correct HKD cost sat in `list_unstated_costs` and a vendor-wide answer of yuan would have overwritten it and moved its shelf price. A fifth fault was found in the same place: the update path stamped BEFORE its unknown-field gate, so all three product command routes refused any edit naming a cost currency with a 400 about a column the server had just added itself. One helper decides the answer now, the mirrors copy it rather than re-deriving it, and the guard refuses a new insert that names a currency without it.

- [x] The wholesale catalogue quotes cost times markup as a per-gram price without dividing by quantity - done 2026-09-09 (fix(network-catalog): divide the curator's cost by quantity before the markup). MONEY-3: 89 of 92 reachable listings, up to 2,500x. `handleNetworkCatalog` now selects `product_listings.quantity_purchased` and divides `curator_cost_amount` by it before applying the markup, matching the shape `addPricingFields` already used for the shop's own catalogue. A profile with no recorded quantity now returns no cost-based price rather than pricing the whole batch as a single gram. Covered by `worker/tests/network-catalog-per-gram.test.ts`.

- [x] Nothing on a push to main runs the type check, the build or the full test suites; add the gate that would have caught the two-day outage - done 2026-09-09, `ci(playwright): the gate lands, six known failures quarantined visibly`. TEST-1, TEST-2: `deploy-frontend.yml` was manual, `playwright.yml` ran 2 of 48 specs, no hooks. `.github/workflows/playwright.yml` now runs, on every push and pull request to main: `checks` (tsc, the color lint, the production build, the worker suite), `mobile` (unchanged), `e2e` (the full browser suite on Desktop Chrome and Mobile Chrome, dist built first), `china-scan`, `platform-hardening` and `recovery` (the three suites that had no CI lane at all). Reproduced the outage by reintroducing the two JSX comments from the parent of `2f1b7ea3`: `npm run lint` and `npm run build` both fail exactly as they did on 2026-09-06 through 2026-09-08. `tea-reference-preview.spec.ts` and `tea-reference-revision-workflow.spec.ts` stay out (already excluded by `playwright.config.ts`'s own `testIgnore`, and the preview spec needs a private, non-CI handoff file). Running the full suite for the first time surfaced six specs failing independently of this change (`compass-capture.spec.ts:498`, `inventory-scroll.spec.ts:220` and `:275`, `shop-refinement.spec.ts:113`, `smoke.spec.ts:44`, `tasting-note-curation.spec.ts:87`). Reproduced again on this checkout: the six run and fail (11 of 12 runs, one flaky pass on `inventory-scroll.spec.ts:220` under Mobile Chrome, matching the original count). Their titles now sit in the workflow-level `KNOWN_FAILING_E2E` regex; `e2e` runs with `--grep-invert` against it so every other test, including the rest of `inventory-scroll.spec.ts`, still blocks a merge (proved by running that file alone with the same invert: 11 passed, 1 skipped by its own project guard, the two known-failing titles correctly absent), and a new `known-failing` job runs the same six with `--grep` and `continue-on-error: true` so they stay visible rather than going quiet. Each of the six is also filed under TODO.md's Untriaged section with what a visitor or operator would see broken; the two-Save-buttons, empty-currency-select, missing-dollar-figure and missing-sticky-search-bar lines read as real page defects rather than stale tests, and the other two (the stock page's missing selection-toolbar actions, the promoted note's missing byline) stay unclear pending a look at the current page.

  A second pass fixed four things a reviewer found on the first landing. The `e2e` job now shards four ways (`--shard=N/4`, `fail-fast: false`, `timeout-minutes: 25` per shard) because the unsharded suite measured 35.0 minutes of playwright time alone on faster hardware than this private repo's 2 vCPU runner tier gets, well past the original 30-minute cap; `known-failing` is unaffected. The `recovery` job now runs `npm run build` once before Playwright starts, so `playwright.recovery.config.ts`'s own `npm run build && npm run preview` webServer, left untouched, warms into a 120-second budget instead of racing it cold. Worker types remain ungated: `npm run lint` covers `src` only and `wrangler deploy --dry-run` strips types via esbuild, so a type error in a worker file no test imports still lands green; `tsc -p worker/tsconfig.json` reports 35 pre-existing errors across seven files today (`curateImportAnalysis.ts`, `curateImportCanonical.ts`, `curateImports.ts`, `eventArticleDraft.ts`, `eventDomain.ts`, `index.ts`, `mcp.ts`), filed as its own TODO line rather than fixed here. And the archive line you are reading used to say two of the six looked like real page defects where TODO.md said four; it now says four, matching TODO.md.

- [x] Draft articles are readable at their direct URL; hide the fourteen article pages from visitors until they are published (JOBC-2), done 2026-09-09, `Gate every surface that names a Read article on one check`. `ReadIndex.tsx` was the only place that knew which pieces were live, and it only decided what the contents list showed, so each of the fourteen pages was fully readable at its own URL: a visitor with empty storage could read the whole Rock Remembers interview at `/read/rock-remembers`. The live map moved into `src/pages/read/articleLive.ts` and every surface that NAMES an article now asks it: the route itself through a new `ArticleGate` in `App.tsx`, the contents index, the "More from The Art of Tea" rail at the foot of each piece, and the crawler-facing head meta the Pages function writes. Flipping a piece live there publishes it everywhere in one edit. `/read/leaf-to-liquor` and the index itself sit outside the map deliberately, named once in `UNGATED_READ_PATHS` rather than remembered separately by three callers. Two rounds of review found the surfaces the first pass missed. The rail was the worst of them: it was a hand-curated array per page with no publish check, so `/read/porcelain-and-tea` advertised three unpublished pieces by title and blurb and every one of those cards was a dead link onto the not-found page the gate had just started serving. Also closed: an ungated route written across several lines was invisible to the route sweep, a crawler-meta entry with no live-map entry kept its own title and description, and a draft still told crawlers its own URL was canonical. The owner check was over-granting too, letting any curator who owns their own shop read every Teajia draft; it now takes the Teajia account, its publish bundle, or a platform role. What this does NOT do: it hides a draft from a visitor, it does not make one private. The text still ships in the public JS bundle and the owner check runs in the browser, so anyone determined can still read an unpublished piece. That is recorded as its own open item in `TODO.md`, because closing it means moving the check and the content server-side.

## Orders and payments

- [x] The phone suite watches the screen edge, not just the page width - done 2026-08-31. The old check compared the whole page against the screen, so an element overrunning its column was absorbed without the page growing and passed unseen: the order summary rendered 551px inside a 311px column and carried the amount 208px off a 375px phone while the page measured exactly 375px. The new check asks whether anything a person can see reaches past the right edge, counting text and painted backgrounds only, because the blunt version flagged a shop tab strip whose box overhangs by 12px while painting nothing there. Surveyed across all twenty swept routes first and clean on every one, then promoted into the shared health assertion so it covers them all rather than the two money pages it started on.

- [x] Every way of being owed money ends at the same record — done 2026-08-31. Wholesale wrote two invoices carrying a status the app does not have, no line items and no amount, with the two money columns on the same row contradicting each other, so the ledger, the pay link and the attention list could not see the money at all. They are ordinary priced invoices now, marked fulfilled because receiving is the goods changing hands and unpaid because receiving tea is not paying for it, and receiving is refused outright when a line's currency has no exchange rate rather than invoicing it at par. The events half of that item was wrong and was read off the schema: `event_attendees.payment_status` is dead, nothing in the worker reads or writes it, and event money already runs through ordinary `EVT-` invoices. What is actually true about events is filed separately.

- [x] A sample request is recorded whether or not the tea is in the shop — done 2026-08-31. A line may be a named custom line with no product, the shape converting already produces for a retired tea. The product-id requirement had been doubling as the anti-junk gate on a public write, so the gate is stated properly instead: a custom line must say it is custom, at most 50 lines, a name at most 120 characters, a note at most 2000. Three of those four are tighter than what shipped before.

- [x] An order is not sent while a line sits at no price — done 2026-08-31. Draft to pending is the only path that makes an order a real ask, and it refuses there, naming the lines so the screen asks about them by name. "Send anyway" is one click, because the operator is the one who knows whether a line is meant to be free.

- [x] The pay page says which order the money is for — done 2026-08-31 → Plan: [order-process-handoff.md](plans/archive/order-process-handoff.md). It uses the key the customer already holds rather than a new one: they arrive from their own order page, so the same key one step later reveals nothing new, and a stranger who guesses a reference code still sees exactly what they saw before. The token never enters the address, because this page renders its own address as a QR code and hands it out under a heading inviting the visitor to share it. Four further problems were found and fixed in the same pass: the order body was staying out of durable browser storage only by a lucky substring match, the one line wiring the feature up had no test and deleting it left everything green, the recipient was not checked alongside the shop so a hand-edited link could show a real order above the wrong person's bank details, and the customer's private key was reaching the incident log through a path recorded verbatim. A fifth was found by opening the live page on a phone: the block sized itself to the longest tea name and pushed the amount 208px off a 375px screen, invisible to every test because the page reported no sideways scroll.

- [x] Close the loop from an order request to being paid — done 2026-08-31, live at teajia.com. An order request now reaches Adrian by email instead of waiting to be noticed, becomes a priced invoice in one click instead of being retyped, and carries a pay link resolved to the right tea master with the amount and a reference to quote. Payments got their own records, so a customer can report a transfer and Adrian confirms it against his bank: a report is never money, part payments work, and the pay link asks for the balance outstanding rather than the full amount again. What the customer sees on their tracking page is now derived from the order itself instead of a status kept by hand, which was generating the "where is my tea" messages the page exists to answer. What needs Adrian gathers into one list in Your Table, oldest waiter first, and six voice commands reach all of it. Fixed along the way: the fulfilment email had never sent because it looked for a column that does not exist and pointed at a domain Teajia does not own; marking an order paid the old quick way now leaves a ledger row so history cannot contradict status; sample requests and AI-prepared orders were vanishing into WhatsApp unrecorded; the cart survived checkout so an order could be sent twice; and the baseline schema carried a Cloudflare internal table that made a fresh database fail to build. Three rounds, ten agents. Remaining work is in [order-process-handoff.md](plans/order-process-handoff.md), which stays live. → Plans: [order-process.md](plans/archive/order-process.md), [round two](plans/archive/order-process-round-2.md), [round three](plans/archive/order-process-round-3.md), [cross-agent notes](plans/archive/order-process-notes.md)

- [x] Stop writing the payment ledger rules twice — done 2026-08-31. Eleven pieces of the money code had been copied from `worker/src/index.ts` into `worker/src/mcp.ts` under a "keep in sync" note, because index imports mcp and the reverse would close a module cycle, so a change to what counts as paid had to be made in both files. They now live in `worker/src/invoiceDomain.ts`, which imports neither, and both import from it: PAYMENT_EPSILON, the text limits and caps, roundUsd, paymentTextField, formatInvoiceNumber, loadInvoiceLedgerTotals, invoiceMoney, loadLedgerInvoice, reconcileLedgerWithColumn, recomputeInvoicePaymentStatus. The same pass closed the one place the two implementations disagreed: `whats_waiting` required `status = 'Draft'` across both halves of its unpriced question, so a Pending order carrying a line priced at nothing was on the screen and not in the spoken answer. `worker/tests/attention-parity.test.ts` now asserts the screen and the voice return the same list, and fails if either drifts.

## Curate

- [x] Make Curate deletes survive being offline — done 2026-06-29 via tombstones: a deleted id is recorded locally and filtered out of hydrate so the still-on-server row can't reappear; the delete is retried on each hydrate until the server confirms it. Fixes "I deleted it and it came back."

## Read / Magazine

- [x] Build the immersive article reader that replaces the 4:5 carousel as the default, where writing leads and visuals plus text-effects punctuate it, reading great on phone and desktop _(done 2026-06-17 — AR.0–AR.6 shipped: scroll-driven reader, section families, ~12 text-effects, block-stack authoring, share-card generation; merged into main)_ → Plan: [immersive-article-system.md](plans/archive/immersive-article-system.md)

- [x] Run the prime-time audit: every lane, verified findings, one ranked page (2026-09-09) → [docs/AUDIT-2026-09.md](../docs/AUDIT-2026-09.md), plan kept at [todo/plans/prime-time-audit.md](plans/prime-time-audit.md)

- [x] Remove the streak counter from the Learn curriculum - done 2026-09-09, "Remove the invented streak counter from Learn curriculum". OPS-3: `calculateStreak` in `LearnCurriculum.tsx` turned watched stories into a "day streak" by dividing the count by two, a number nobody chose sitting next to the real completed count. Streak trackers are on CLAUDE.md's DO NOT build list. Removed the function and its render; nothing else in `src` computes or shows a streak, checked by grepping for gamification patterns (badge, gamif, achievement, points) across `src` and `worker/src`, all of which turned out to be ordinary status pills or content-difficulty labels. Guarded by `worker/tests/no-streak-tracker.test.ts`, which scans every file under `src` for the word outside comments.
